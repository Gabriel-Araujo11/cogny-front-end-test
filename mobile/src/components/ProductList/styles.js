import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    marginTop: 20,
    marginBottom: 13,
    alignItems: "center",
  },
  text: {
    color: "white",
  },
});
